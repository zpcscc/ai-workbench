[package]
name = "ai-workbench"
version = "0.1.0"
description = "Local-first AI workbench"
authors = ["AI Workbench"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

